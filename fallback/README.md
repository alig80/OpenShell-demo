# Fallback Recording

`demo.cast` is a tiny asciinema v2 recording of the deterministic scripted flow. Use it only if the terminal layout fails right before going on stage.

```bash
asciinema play fallback/demo.cast
```

Narrate the same four beats over it: allowed read, denied write, policy reload, allowed retry.
